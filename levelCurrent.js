level.current = 1;

function levelCurrent() {
    if (level.current === 1) {
        levelLoad(level.one)
    }

    if (level.current === 2) {
        levelLoad(level.two)
    }

    if (level.current === 3) {
        levelLoad(level.three)
    }

    if (level.current === 4) {
        levelLoad(level.four)
    }

    if (level.current === 5) {
        levelLoad(level.five)
    }

    if (level.current === 6) {
        levelLoad(level.six)
    }

    if (level.current === 7) {
        levelLoad(level.seven)
    }

    if (level.current === 8) {
        levelLoad(level.eight)
    }

    if (level.current === 9) {
        levelLoad(level.nine)
    }

    if (level.current === 10) {
        levelLoad(level.ten)
    }
}
